import 'package:flutter/material.dart';

class MedicalHistory extends StatelessWidget {
  const MedicalHistory();
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Enter Medical History"),
    );
  }
}
